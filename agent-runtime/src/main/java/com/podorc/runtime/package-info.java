/**
 * Agent runtime territory: {@code agent.yaml} loading, LLM provider adapters (Claude / Codex
 * CLI subprocess), deterministic prompt assembly, and the inbound/outbound processing loop.
 *
 * <p>Empty in S1-01; populated from S1-05 onward.
 */
package com.podorc.runtime;
