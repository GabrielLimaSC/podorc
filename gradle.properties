# Single source of truth for shared dependency versions.
springBootVersion=3.4.1
# Ahead of the Spring Boot 3.4 BOM (1.20.x): its bundled docker-java cannot talk to
# Docker Engine 29 (API 1.54) and fails every client strategy with HTTP 400.
testcontainersVersion=1.21.4

org.gradle.parallel=true
org.gradle.caching=true
